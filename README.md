## Setup

```
git clone https://github.com/keboola/processor-skip-files
cd processor-skip-files
build . -t processor-skip-files
cd ..
git clone https://github.com/keboola/processor-oom-test
cd processor-oom-test
./run.sh
```